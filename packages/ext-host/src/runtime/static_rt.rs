//! Static runtime: extensions compiled into the binary.
//!
//! Zero overhead, full Dioxus access for panels, and the path every built-in
//! editor takes. The only "sandbox" is the permission check in `Host`.
