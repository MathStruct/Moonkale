// @moonkale/milkdown — thin wrapper. See ../PROTOCOL.md and ../../README.md.
//
// Exposes window.moonkale.milkdown = { mount, apply, set*, destroy } and emits
// CustomEvent("moonkale:milkdown") on the mount element. No application logic.
// Not implemented yet.
export {};
