// @moonkale/xterm — thin wrapper. See ../PROTOCOL.md and ../../README.md.
//
// Exposes window.moonkale.xterm = { mount, apply, set*, destroy } and emits
// CustomEvent("moonkale:xterm") on the mount element. No application logic.
// Not implemented yet.
export {};
