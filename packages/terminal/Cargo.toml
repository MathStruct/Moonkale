[package]
name = "moonkale-terminal"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Terminal session model and backend trait (no PTY here). Runs on all platforms."

[dependencies]
futures-channel = "0.3"
serde = { version = "1", features = ["derive"] }
regex = "1"
