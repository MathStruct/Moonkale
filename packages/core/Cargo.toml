[package]
name = "moonkale-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Platform-agnostic domain model: the graph-native document, sources, commands and events. No I/O."

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "v5", "serde", "js"] }
async-trait = "0.1"
thiserror = "2"
serde_json = "1"

[dev-dependencies]
