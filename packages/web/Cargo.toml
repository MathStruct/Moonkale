[package]
name = "web"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["fullstack"] }
ui = { workspace = true }
api = { workspace = true }
moonkale-core = { workspace = true }
moonkale-llm = { workspace = true }
tracing = "0.1"

[features]
default = []
web = ["dioxus/web"]
server = ["dioxus/server", "ui/server", "api/server"]
