[package]
name = "moonkale-sources-sql"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "SQL sources: Postgres/Supabase, SQLite, DuckDB, Turso/libSQL. Native only."

[features]
default = []
postgres = []
sqlite = []
duckdb = []
turso = []

[dependencies]
# moonkale-core = { workspace = true }
# moonkale-sources = { workspace = true }
# sqlx = { version = "0.9", features = ["runtime-tokio", "postgres", "sqlite"], optional = true }
# duckdb = { version = "1", features = ["bundled"], optional = true }
# libsql = { version = "0.10", optional = true }
