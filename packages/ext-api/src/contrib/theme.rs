//! `ThemeContribution` — a set of CSS custom properties (`--wb-*` for the
//! workbench, `--mk-*` for editors, plus a syntax-highlight token palette
//! shared by the code editor, the markdown code blocks and the graph view's
//! node colours). Themes are data, never code.
