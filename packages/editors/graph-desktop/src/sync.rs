//! Geometry sync: observe the placeholder `<div>` in the panel
//! (ResizeObserver and scroll via `document::eval`), and move, resize, show or
//! hide the native surface to match, including when the workbench docks the
//! panel elsewhere or a tab in front of it is selected.
