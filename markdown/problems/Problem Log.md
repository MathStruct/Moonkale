---
title: "Problem Log"
tags: [problems, log]
---
Running log of problems hit during implementation. One note per problem (`P-nnn Title`), created from [[Problem Template]]. Numbers continue from the [[Problem Ranking]] where the problem was anticipated, or take the next free number.

| ID | Title | Status | Phase |
|---|---|---|---|
| [[P-001 Graph surface in desktop webview]] | Where does the wgpu surface come from inside the desktop webview? | open | 2 → 5 |
| [[P-002 Dioxus mount point and full-height layout]] | `#main` mount div broke the full-height workbench | resolved | 0 |
| [[P-003 dioxus-workbench theme variables]] | Dark palette override didn't apply | resolved | 0 |
| P-032 Release-build observability | No inspector/stdout in compiled desktop builds → file log, panic hook, JS error forwarding, Diagnostics panel ([[Debugging and Logging]]) | open | 1 |
| P-033 WebKitGTK WebGPU flag | Enable `WebGPU` feature via raw `webkit2gtk-sys` call; measure ([[Linux Desktop Setup]]) | open | 2 |
| P-034 Hydration mismatch from `cfg!` in markup | Fullstack SSR renders on the server; a `cfg!(wasm32)` placeholder showed the native text on web. Rule: no compile-time platform branches in rendered output ([[Milestone 1 - Implementation Log]]) | resolved | 1 |
| P-035 `.gitignore` ignored outside git repos | `ignore` crate defaults to `require_git(true)`; set `false` so an editor behaves the same before `git init` | resolved | 1 |
| P-036 Server-function name clash | `#[post] fn query(.., query: Query)` fails: the generated stub calls `query(...)` and the parameter shadows it; renamed with suffixes | resolved | 1 |
| P-038 Desktop link error `-lxdo` | `dx run` in `packages/desktop` fails at link time: `muda` needs `libxdo.so`; install `xdotool` (Arch) / `libxdo-dev` (Debian). Docs had it as optional — corrected in [[Linux Desktop Setup]] | resolved | 1 |
| P-039 No native folder dialog in Milestone 1 | By design (out of scope); now `rfd` (xdg-portal backend) behind `WorkspaceConfig::pick_folder` on desktop, wired to File → Open Folder…, Ctrl+O and the Explorer button. Requires `xdg-desktop-portal` + a backend on Linux ([[Linux Desktop Setup]]) | resolved | 1 |
| P-040 Custom title bar on an undecorated window | Decorations off; drag/resize/minimize/maximize/close via `dioxus::desktop::window()` callbacks passed into `ui::Frame`. Maximize icon doesn't flip; Wayland resize depends on the compositor | resolved (gaps noted) | 1 |
| P-041 u64 through JSON loses precision | `Version(u64)` hashes > 2⁵³ became floats on the `BroadcastChannel` path and failed to deserialize, silently killing the receiver loop. `Version` now serialises as a hex string; receiver loops skip bad messages instead of exiting | resolved | 1 |
| P-042 `Moved` echo check used the wrong field | "ignore my own messages" keyed on `from`, but `Moved.from` is the origin, not the sender — the origin never closed its copy. `SessionMessage::sender()` returns `to` for `Moved` | resolved | 1 |
| P-043 dx serve missed a Rust change | A change in `ext-api` was not rebuilt by a long-running `dx serve` (no rebuild line in the log); restart dx when a non-rsx change seems not to apply | open (tooling) | 1 |
| [[P-045 Cross-window drag and drop]] | Firefox "Server Not Found: wb-tab-editor-…" (unhandled tab drop navigates); desktop drag + folder sync unverified. Tab drag is now the gesture, stray drops inert, desktop seeds sources from the registry, session log + "N windows" status for diagnosis | resolved (desktop confirmed by hand) | 1 |
| P-044 Cross-window drag didn't work by hand | Three causes: (1) Firefox never *starts* an HTML5 drag unless `dragstart` calls `dataTransfer.setData` — Dioxus's synthetic handler can't, so a native listener on the handle now does; (2) web *New Window* opened a tab, and a background tab can't be a drop target — now a popup window; (3) whether WebKitGTK delivers an HTML5 drop across two webviews of one process is unverified, so the offer now survives `dragend` as a **"Move it here" banner** in the other window (grab → release → click). Verified on web with a real mouse drag; desktop still needs a manual run | resolved (fallback) | 1 |
| P-046 Random element ids vs hydration | Graph panel ids derived from the random `WindowId` differed between server render and client; `getElementById` failed silently. Deterministic ids for anything an eval looks up ([[Milestone 2 - Implementation Log]]) | resolved | 2 |
| P-047 Eval messages lost | Sent-before-awaited and sent-then-returned messages are dropped; also an eval mounted from an effect that writes a signal it reads. Pattern: `init` handshake, park on `recv` until `destroy`, mount from `onmounted` | resolved | 2 | **2026-09-20 (spec 016)**: the code editor still mounted from `use_effect` and hung at "Loading editor…" on desktop under load; moved to `onmounted`; the mount script now reports a thrown error into the panel instead of hanging
| P-048 dx serve misses Rust changes (3×) | Non-rsx changes to a `const &str` script were not rebuilt; restart dx each time (~3 min) | open (tooling) | 2 |
| P-049 No GL in headless Firefox; wgpu hangs without a context | Probe WebGL2/WebGPU before `create()`, show the reason; rendering verified only in real browsers | resolved (fallback) | 2 |
| P-050 wgpu 30 API drift | Several descriptor fields/enums changed; read the registry source | resolved | 2 |
| P-051 Graph view jumped back after dragging | Every layout settle re-fit the camera, and node drags reheat the layout; dragged nodes were also unpinned and drifted. Auto-fit now only after a new graph/relayout until the user moves the view; dragged nodes stay pinned | resolved | 2 |
| P-052 New side panel stole the active tab | Attaching a late panel activates it; default layout lists `explorer, links` explicitly | resolved | 2 |
| P-053 `dragend` missing after a workbench drop | Firefox drops the tab on the workbench's own drop zone and never fires `dragend`; a `drop` in the source window now ends the session drag too | resolved | 2 |
| P-054 Typst preview compiled forever | The preview effect read `busy()` reactively and set it after each compile, re-triggering itself; `busy.peek()` | resolved | 3 |
| P-055 Terminal Ctrl+click | Row index must come from `.xterm-rows` geometry; long paths wrap so neighbour rows are joined; xterm + headless Firefox swallow real modified clicks — the E2E dispatches a synthetic `click` | resolved | 3 |
| P-056 dx serve restarts | Non-rsx Rust changes still need a restart of dx (~2–3 min). **Never `pkill dx`**: it also kills Daniel's own `dx serve` (desktop) in another shell — on 2026-09-19 that took his desktop app down while he was trying the flow editor (WebKit exit segfault, P-061 signature, at 12:14 and 12:31). The test server is now started and stopped by PID with `packages/web/tests/e2e/serve.sh` | open (tooling) | 3 |
| P-057 `Websocket<String, String>` in a server fn | The `#[get]` macro turns a bare `String` type parameter into `str` (`DeserializeOwned` not satisfied); wrap in a `Frame(String)` newtype | resolved | 3 |
| P-058 LSP progress text overflowed the status bar | rust-analyzer's `$/progress` messages are `n/m: <long path>`; cut at the first `: ` in `lsp::session` | resolved | 3 |
| P-059 E2E fixture drift | `graph.mjs` saves a wiki-link to `Another` into `Home.md`; a later `links-sqlite.mjs` run sees one phantom too many. Restore the fixture between suites (README). Milestone 6: `fixture.sh` rebuilds the fixture deterministically and the regression run resets `Home.md`/`.moonkale/` per suite | resolved | 3 |
| P-060 Database internals invisible in the graph view | Daniel: the Graph tab kept showing the folder; the source picker defaulted to *index* and *Show in Graph* did not raise the Graph tab (same tile as the table editor). Opening a graph database now selects it in the picker; *Show in Graph* dispatches `Command::ShowPanel("graph")`, which the shell applies via `PanelLayout::reconcile` + `activate` | resolved | 3 |
| P-061 WebKitWebProcess SIGSEGV in NVIDIA EGL (desktop) | Core dumps of `WebKitWebProcess` inside `libnvidia-eglcore`, called from WebKitGTK's compositor. **Pinned down** (2026-09-18): the web process segfaults while *tearing down a live WebGL context* — every time the app exits (graceful close included: Daniel's 13:13 dump was a build with the `pagehide` handler, closed 13 s after launch) or `dx serve` kills it for a rebuild. Not a startup crash; not the DMA-BUF renderer (the crashed process had `WEBKIT_DISABLE_DMABUF_RENDERER=1`); losing the context from JS doesn't help because WebKit's own teardown is what crashes. The process is exiting anyway, so the only effect is a 10 MB dump and a KDE crash popup per run. Fix: `desktop/main.rs` sets the soft `RLIMIT_CORE` to 0 when the NVIDIA driver is loaded (inherited by the WebKit children; `MOONKALE_COREDUMPS=1` keeps dumps for real debugging). Verified: killing the dx-built app produced a dump before, none after; Daniel confirmed no KDE crash popup under `dx serve` (only a `SIGSEGV none` journal line remains, which the kernel writes regardless of the limit) | resolved (confirmed) | 3 |
| P-062 Graph view showed only a database's schema | Daniel expected the actual people/cities with their relations. Databases now have **Data / Schema / Query** modes (Data is the default after opening one): `Query::All { kinds: [Vertex] }` returns nodes + relations from the source; vertices are coloured per label (node table), relations per name, with a legend in the toolbar. Vertex keys carry the label (`v:Person:0:3`) so the host colours without a lookup | resolved | 3 |
| P-063 Graph panel loads raced | Adding a source re-ran the load effect before the picker switched; the slower remote index reply overwrote the trace result. Generation counter: only the newest load publishes | resolved | 4 |
| P-064 Agent borrowed across awaits | Streaming into `&mut Agent` for a whole exchange conflicts with the panel reading its audit log; an audit mirror signal + `busy` fence (clippy lint allowed with the rationale) | resolved | 4 |
| P-065 Global shortcuts need focus inside the frame | The key handler is on the frame `div`; Ctrl+Shift+F did nothing when `body` had focus. Milestone 5: a document-level listener (`GLOBAL_KEYS` eval in `Frame`) forwards Ctrl-combos whose target is the body | resolved | 4 |
| P-066 Port 8080 taken by the desktop dev server | Daniel's `dx serve` (desktop) holds 8080; the web E2E suites take `PORT` and the test server runs on 8090. Also cleaned up leaked `server-*` processes from old dx runs | resolved | 4 |
| P-067 Mistral: unavailable models look rate-limited | Models the key cannot use (`devstral-*`, `mistral-small-latest`) return 429 "Rate limit exceeded", not 404. `mistral-code-latest` / `codestral-latest` + `mistral-embed` work through the OpenAI-compatible provider | resolved (noted) | 4 |
| P-068 Graph nodes lost their red channel | Since Milestone 2 every node looked cyan/teal regardless of kind: `vertex_attr_array!` packed the colour attribute at offset 12 while `NodeInst` pads `radius` to 16, so the shader read `(pad, r, g, b)`. Spotted from Daniel's screenshot where the legend colours and the drawn nodes disagreed. Explicit attribute offsets now; renderer rebuilt; Daniel confirmed the colours on desktop | resolved (confirmed) | 4 |
| P-069 `terminal.run` has no timeout | The tool ends the shell after the command so the output stream closes, but an interactive command waits forever (no timer primitive on wasm without a JS dependency). Output capped at 200 KB. Follow-up: cancel button on the tool card | open | 5 |
| P-070 dx serve served a stale JS bundle | After `npm run build` rewrote `milkdown.js`, the page kept requesting the old content-hashed asset. Restart dx after rebuilding a bundle | open (tooling) | 5 |
| P-071 Hydration shows server-side settings briefly | The Settings panel derives the LLM scope badge from `env_overrides()`, which the server has and the wasm client does not; the server markup stays until the first client re-render (same family as P-034) | open (cosmetic) | 5 |
| P-072 Desktop: last folder not reopened at launch | Daniel: the key was remembered but not layout/tabs. Cause 1: nothing reopened the last folder at start — restore only ran after a manual open. Now `WorkspaceConfig::reopen_last_folder` (desktop `true`) opens `recent_folders[0]` after the user settings load, from the frame's `onmounted`. Cause 2 (suspected, not confirmed): during my desktop runs the index build stalled at startup — but the KDE session was locked at the time and WebKit suspends invisible pages, so those runs prove nothing. Trace lines (`settings: …`, `open_folder: …`) show the sequence in the dx log. Daniel confirmed: the desktop app reopens the last folder as left | resolved (confirmed) | 5 |
| P-073 dioxus-flow handle geometry lags its layout animation | After *Layout* the handle positions used to validate a connection are stale for a few frames, so a drag started right away can miss; also the third palette column sat under the tile splitter at 1500 px. Blocks are placed on a 165 px grid; the E2E wires before laying out. Real fix needs a settled-layout callback from dioxus-flow | open (upstream) | 6 |
| P-074 Controlled workbench layout is rendered reconciled | `PanelWorkspace` renders a *reconciled* copy of the controlled signal: newly attached panels and the auto-activated document tab appear before any write to the application's signal, so reading the front tab from the signal was stale. The phone bar reconciles with the current placements before reading or activating (as `ShowPanel` already did) | resolved | 6 |
| P-075 dioxus-flow debug warnings in the desktop log | With a flow open, dioxus-flow 0.1.2 logs `Changing the props of Style {}` (its `document::Style { {STYLE} }` re-renders) and `A Copy Value created in ScopeId(…Canvas) … used in …Flow` (its `Canvas` signals are read by the parent `Flow` through `attach_core`) on every render — 15× / 14× in Daniel's `error7.md`. Both are upstream, debug-only and harmless here (no failed reads observed); the file's abrupt end was dx's `SIGKILL` relaunch after a Rust edit, not a crash. Worth an upstream report with the next dioxus-flow issue (P-073) | open (upstream) | 6 |
| P-076 Spawned task dropped with its component | The Explorer's inline field set `edit = None` (unmounting itself) and then `spawn`ed the create/rename; the task died with the scope and nothing happened, silently. `spawn_forever` for work that must outlive the component that starts it | resolved | 7 |
| P-077 `serde(rename_all)` on an enum does not rename variant fields | The CodeMirror bridge sent `endLine` for `CodeActions`, Rust expected `end_line`; the parse error ended the bridge's `recv` loop so every later editor event was lost. Per-variant `rename_all` now, and an unreadable message is logged and skipped | resolved | 7 |
| P-078 Completion source never asked | The bundle's completion source registered its resolver and forgot to call `onCompletion`; nothing visible failed. Found by loading the bundle in a bare HTML page (`cm-test.html`) — the fastest way to test the JS side without the app | resolved | 7 |
| P-079 Click before hydration after a form redirect | After `/login` the server-rendered page is shown before the wasm hydrates; a click on *Open* in that window does nothing. Tests wait for `networkidle`; a loading hint for humans is a follow-up | open (cosmetic) | 7 |
| P-080 The type-mismatch diagnostic is cargo-check's, not rust-analyzer's | Driving rust-analyzer 1.98 directly (`ra-probe.py`): `didOpen` yields the `mismatched types` error from flycheck (cargo check), and `didChange` republishes only native diagnostics (syntax errors) — so fixing the type error does not clear the gutter until the next check, which runs on save. `lsp.mjs` now saves; the code panel's `Ctrl+S` path sends `didSave` like the toolbar button (it did not before — one `save_now` callback for all three paths) | resolved | 7 |
| P-081 Long E2E batches time out under load | In a 23-suite batch, `phone` and `auth` hit 30 s timeouts that never reproduce alone or in pairs: by then several rust-analyzer instances (from `lsp`/`lsp2`) and, for `auth`, a second dev server share the CPU. Both pass on rerun; treat a single timeout late in a batch as load, rerun the suite, and only then debug | open (tests) | 7 |
| P-082 Firefox headless has no WebGL | Even with `webgl.force-enabled` and software rendering prefs, headless Firefox exhausts its GL drivers; the graph renderer was never exercised by a test until Milestone 8. Chromium headless with `--use-angle=swiftshader --enable-unsafe-swiftshader --ignore-gpu-blocklist` provides WebGL2 (`graph3d.mjs`); the GL canvas cannot be read back after present, so the suite reads the label overlay and compares screenshots | resolved (tests) | 8 |
| P-083 Playwright `:has-text()` is a substring match | `label:has-text('Name')` matched "Secret name" first and the test typed a user name into the secret field; use `:text-is()` for exact labels | resolved (tests) | 8 |
| P-084 wgpu 30 depth-state fields are `Option`s | `depth_write_enabled: Some(true)`, `depth_compare: Some(CompareFunction::LessEqual)` — the docs' examples still show plain values | resolved | 8 |
| P-085 Edits anchored on stale text | Multi-edit scripts kept failing after `cargo fmt` reshaped the anchors; each retry cost minutes. Rule: re-read the exact lines right before editing, one file per script, and format last | open (working rule) | 9 |
| P-086 Compaction hides old renames | After `EntityLog::compact`, renames before the boundary are folded away; `text_at` correctly starts from the snapshot keyed by the current id, but the History panel's key lookup for very old events relies on `Event.key` (carried since Milestone 8) | noted | 9 |
| P-087 Android WebView drops head elements from the first render | `document::Stylesheet` in a panel that is part of the first frame never reaches `<head>` on Android (the Explorer, Agent and Git panels were unstyled; panels mounted later were fine). Desktop and web are unaffected. Fix: `moonkale_ext_api::Stylesheet` — an effect that inserts the `<link>` itself through `document::eval` if it is missing, re-run when `Workspace::assets_epoch` bumps (the frame bumps it from `onmounted`). Every panel uses it now | resolved | 9 |
| P-088 `import()` of an asset URL fails on Android | The renderer's `import(JS_URL)` threw "base URL is about:blank because import() is called from a CORS-cross-origin script": eval scripts on the WebView have no base URL. Fix: resolve every asset URL against `document.baseURI \|\| location.href` before `import()` and `init({ module_or_path })` | resolved | 9 |
| P-089 WebGPU device creation never completes on the Android WebView | WebView 125 advertises `navigator.gpu`, `requestAdapter` succeeds, and `create()` then hangs (no error, no timeout from wgpu). Fix: the Graph panel passes `prefer: "gl"` when the UA is an Android WebView (`Android` + `wv`), and `create(canvas, overlay, cb, prefer)` → `Renderer::new(.., gl_only)` skips WebGPU. WebGL2 renders the graph at full speed for the vault sizes a phone holds | resolved | 9 |
| P-090 sRGB surface format washes the graph out on GL | On WebGL2 the surface reported `Rgba8UnormSrgb` first; the shaders write linear colours, so the background came out grey and the nodes pale. Fix: `Renderer::new` picks the first non-sRGB format from `surface.get_capabilities` (falls back to the default). WebGPU on desktop/Chromium is unchanged (it already chose non-sRGB) | resolved | 9 |
| P-091 Graph auto-fit ran before the canvas had a size | On the phone the Graph tile is laid out after `create()` runs, so the layout settled and `Camera::fit` used a 1×1 canvas: scale clamped to 0.05, every node at the centre (screenshot `android10`). **Fit** fixed it by hand. Fix: `resize()` re-fits while `auto_fit` is still set (nobody has panned or zoomed), which also keeps a resized panel fitted | resolved | 9 |
| P-092 dx leaves stale hashed assets in the Android project | Every `dx build --platform android` adds the new `<name>-dxh<hash>` asset to `target/dx/mobile/release/android/app/app/src/main/assets` without removing the previous one: after five renderer rebuilds the APK carried five `graph_render_bg.wasm` copies (38.8 MB instead of 23 MB). Workaround: delete that `assets` directory (or `dx clean`) before a release build; a release checklist item in [[Android]] | noted | 9 |
| P-093 Highlighting grammars live in the CodeMirror bundle | The rule "no language knowledge in JS" ([[JS Interop Boundary]]) assumed tree-sitter highlights from Rust; tree-sitter is native-only and the web client would need a server round trip per keystroke. Decision: highlighting is presentation — Lezer/legacy grammars ship in the bundle (`languages.ts`, +650 KB), Rust still picks the language id; symbols, links, LSP stay in Rust. The Rust-native editor ([[JavaScript Inventory]]) brings its own highlighter when it comes | decided | 10 |
| P-094 `onload` on `<img>` never fires in dioxus | `load` does not bubble, and dioxus delivers events through one delegated listener; the image viewer's natural size stayed unknown. Fix: `onmounted` + an eval that awaits `img.decode()` and *returns* the size (an eval is a function body — an IIFE expression without `return` yields `undefined`, the second half of the same hour) | resolved | 10 |
| P-095 The per-session token showed up in the ssh terminal tab | The remote script printed `MOONKALE_TOKEN?` and *then* ran `stty -echo`; the desktop wrote the token as soon as it saw the prompt, the PTY still echoed, and the hex line appeared in the tab (and in the tee stream). Fix: echo off first, then the prompt, then `read`; the shim test's tee output shows no token now | resolved | 11 |
| P-096 `dioxus::server` panics without a `public/` directory | `serve_dioxus_application` reads `./public` at startup (`Couldn't read public directory … No such file or directory`) even when only the API is used; the uploaded server died silently until the tee output was printed. Fix: the remote script `mkdir -p "$d/public"` before `exec`; an empty directory is enough (the server then serves a default `index.html` and the API) | resolved | 11 |
| P-097 No display to run the desktop app on this machine | The only X server on `:0` is sddm's login screen (Daniel works through code-server); no Xvfb/weston. Consequence: desktop-only UI (the remote dialog) is verified by Daniel, and workspace logic behind it gets a `VirtualDom` harness test (`ext-api/tests/remote_flow.rs`: signals, `spawn`, phases — 1.2 s) — a pattern for later desktop-only features | decided | 11 |
| P-098 dioxus's server URL is a `OnceLock` — `set_server_url` after launch panics | `dioxus::launch` (desktop, fullstack) sets `ROOT_URL` to the dev-server address once; `api::client::connect` calling `set_server_url` for the SSH session panicked on the handshake thread (`called Result::unwrap() on an Err value: "http://127.0.0.1:33581"`), so the UI stayed at "starting the server…" — found on the real desktop once a display was available (the shim test set the URL first and never saw it). Fix: `api::relay` — a loopback TCP listener installed *before* launch as the permanent server URL, piping every connection (server functions and typed websockets alike) to the remote that is active, TLS with the WebPKI roots for `https://`; `connect` only sets the headers and the target. Unit test in `relay.rs`; the shim test and the desktop test install it the same way | resolved | 11 |
| P-099 The desktop under my shell's environment: Wayland protocol error 71, then a black WebKit window on X11 | Launched with the shell's inherited variables the GTK Wayland backend died ("Error 71 (Protocol error) dispatching to Wayland display"); with `GDK_BACKEND=x11` WebKit printed "Failed to create GBM buffer … Invalid argument" and rendered nothing (`WEBKIT_DISABLE_DMABUF_RENDERER=1` renders, at +250 MB). Launching with `env -i` and only the session's own variables (`WAYLAND_DISPLAY`, `DISPLAY`, `XAUTHORITY`, `XDG_RUNTIME_DIR`, `DBUS_SESSION_BUS_ADDRESS`) works natively on Wayland — the recipe for running the app from a script. The graph canvas looked black in those runs — not a rendering fault: P-100 | noted | 11 |
| P-100 A big graph could not be seen whole — the zoom floor was too high | The Moonkale repository's graph (2 851 nodes, ideal edge length 28 + 6·ln n ≈ 76 world units) spans more world units than a 0.05 minimum scale can put on a 700 px canvas, so `fit` clamped to 0.05 (only the middle of the graph in view — and the middle of a ring-shaped layout is empty, hence the "black" canvas of P-099) and the wheel could not zoom out further; Daniel zoomed out and hit the same floor. Fix: `MIN_SCALE = 0.002` for both; the view also re-fits on every frame while the layout is still spreading, not only at settle. Verified in the desktop app: the whole graph, isolated-node ring included, fits the panel | resolved | 11 |
| P-101 A desktop `dx serve` is two gigabyte builds | A fullstack desktop app builds the client *and* a server binary (`target/dx/moonkale/debug/web/server`, 1.7 GB in debug) even though the desktop never runs that server; with `debug = 2` (the default) each link is a gigabyte-plus and a feature change costs both builds — the 1 767 s Daniel saw after a pull (spec 023). Mitigation: `[profile.dev] debug = "line-tables-only"` + no debug info for dependencies; my own cargo runs use `CARGO_TARGET_DIR=target/agent` so they stop invalidating `target/debug` (178 GB, 2 370 incremental dirs when measured). Open: build the desktop without the server half (dioxus configuration) | investigating | 11 |
| P-102 Opening a markdown file in Rich mode marked it dirty | Milkdown's parse → serialize round trip normalises bullets, escapes and blank lines and fired `markdownUpdated` once at load; the panel took that as an edit. Fix: the normalised text becomes the baseline after `crepe.create()` (reporting suppressed during load), only user edits are reported (spec 021) | resolved | 11 |
| P-103 The remote command goes through the host's *login shell* | `ssh host -- sh -c '<script>'` is parsed by whatever shell `/etc/passwd` names on the host — bash, zsh, fish or nushell — and only POSIX shells understand `'\''`; on this machine (login shell nushell) the command did not even parse. Fix: the script travels as base64 inside the one argument every shell passes through unchanged (a single-quoted string without quotes or backslashes): `sh -c 'eval "$(echo <b64> \| base64 -d)"'`; `sh` decodes and runs it with the PTY still on stdin. Verified against a real `sshd` with nushell as the login shell | resolved | 11 |
| P-104 Real `ssh` without `-t` echoed the token and let the remote server outlive the session | Without a remote PTY the script's `stty -echo` is a no-op and the local PTY echoes what the desktop types (the token appeared in the tab, P-095 again); and killing the master `ssh` did not kill the remote server (no SIGHUP — it lingered after the session). Fix: `-t`: a remote PTY, raw mode locally, SIGHUP on close; verified with the private sshd (0 lingering processes after six sessions) | resolved | 11 |
| P-105 Remote port collisions when host and desktop are the same machine | The remote port was derived from the local one and landed in the ephemeral range, where a busy machine has thousands of TIME_WAIT sockets (16 000 here after a test batch): 3 of 5 sessions failed with "Address already in use". Fix: a random port **below** the ephemeral range (20000–31999); a real listener there is the rare remaining case and the terminal tab shows the bind error | resolved | 11 |
| P-106 "Stuck at MOONKALE_NEED_UPLOAD" in the app started by `dx serve` | Daniel's first real session: the host reported it had no server, and nothing happened. `dx` runs the app from `target/dx/<app>/debug/linux/app`, so `server_binary()`'s relative lookup found no dev server binary; the session went to `Failed` with only a status-bar line, while the host's script kept waiting for the upload (up to 10 min). Fix: the lookup walks up from the executable and the cwd to the workspace root (`[workspace]` in `Cargo.toml`) and takes `target/dx/web/{release,debug}/web/server`; a failure is now also printed into the `ssh` terminal tab (`[moonkale] remote session failed: …`) and the master `ssh` is killed so the host stops waiting; the message says how to build a server binary. Verified: the release app launched from its bundle dir uploads and connects against the private sshd | resolved | 11 |
| P-107 Two `dispatch`es in one tick keep only the last | `Workspace::commands` is one `(seq, Option<Command>)` signal read by effects; *show the terminal panel* followed by *new terminal* in the same handler left the panel without a session (the second write replaced the first before any effect ran). Fix: `ui::frame::open_terminal_in` shows the panel, yields two renders (`YieldNow`, a future pending once), then dispatches `NewTerminalIn`. A queue on `dispatch` would be the general fix; noted for when a third case appears | resolved | 12 |
| P-108 A task spawned from a closing dialog is cancelled with it | `spawn` in the terminal chooser's click handler belonged to the dialog's scope; `open.set(false)` unmounted it and the delayed dispatch never ran. `dioxus::core::spawn_forever` for work that outlives the component that starts it | resolved | 12 |
| P-109 `#[component]` props need `PartialEq` — `Rc<RefCell<…>>` and fn-pointer structs | the macro compares every prop: `NativeSession` got a by-id `PartialEq`, `AgentSessions` (function pointers, one set per platform) a constant `eq` — comparing fn pointers is a lint for good reason | noted | 12 |
| P-110 Edits after `cargo fmt` silently missed | two scripted text replacements targeted code rustfmt had reflowed in between and did nothing: the `agent.on_server` gate and the terminal routing each shipped once as a no-op until a browser suite showed the old behaviour. Verify a replacement changed the file (grep after), format before editing | noted | 12 |
| P-111 `makepkg` links fail with undefined `duckdb_*` / `ring_core_*` symbols | Arch's default `makepkg.conf` has the `lto` option (`-flto=auto` in CFLAGS/CXXFLAGS): the C/C++ objects that DuckDB's and ring's build scripts compile become GCC LTO bitcode inside their static archives, which `rust-lld` cannot read. Fix: `options=('!debug' '!lto')` in `packaging/arch/PKGBUILD`; `-bin` has no build step. The same applies to any Arch package of a Rust crate with C++ dependencies | resolved | 13 |
| P-112 The Rust terminal cut off its lower rows on desktop | The grid was sized from one `get_client_rect` at mount; when the bottom tile got its real height later, no `onresize` arrived in the webview, so rows (and the PTY size) stayed at the earlier, larger value and the bottom of the screen — the prompt — was outside the panel. Fix: re-measure on a 600 ms timer as well; the screen is bottom-anchored so an oversized grid drops top rows first | resolved | 14 |
| P-113 Two tree-sitter runtimes cannot share a binary | `dioxus-code` (the Rust editor) uses `arborium-tree-sitter`, `moonkale-index` used the `tree-sitter` crate; both declare `links = "tree-sitter"` and Cargo refuses ("only one package in the dependency graph may specify the same links value"). Resolution: the index uses arborium's runtime and grammars (`arborium-rust/-julia/-python`, standard `LanguageFn`s, identical node kinds — the extractors' tests pass unchanged). Side effect worth having: the index's parsers now compile to wasm too | resolved | 14 |
| P-114 wasm link: `undefined symbol: stderr` from tree-sitter's `alloc.c` | arborium's own `wasm.rs` stubs malloc/free/str*/clock…; `stderr` and `fprintf` live in `arborium-sysroot`, which no Rust code references, so rustc never links it (an unreferenced dependency is dropped). `cargo check` passes, only the link fails. Fix: `editors/code-native/build.rs` compiles `csrc/wasm_stubs.c` (the two symbols) on wasm32 | resolved | 14 |
| P-115 The `lbug` (LadybugDB) crate downloads its C++ library at build time | Its build script fetches `liblbug-static-linux-<arch>-<variant>.tar.gz` from GitHub (or builds Kuzu's C++ with CMake, ~1 h) — fine on a developer box, impossible in the Nix sandbox and in a clean-chroot `makepkg`. Fix for Nix: a fixed-output derivation of the archive (`flake.nix`: `liblbug`) + `LBUG_LIBRARY_DIR`/`LBUG_INCLUDE_DIR`, which the build script honours ("external" mode). For the AUR's clean-chroot build the same env pair would point at a `source=` entry with a checksum — not done yet (`makepkg` with network works). Nix-built GTK apps on a non-NixOS host additionally need `nixGL` for EGL | resolved | 13 |
| P-037 Whole-document change events | JS reported the full text per keystroke; O(n) per key. **Resolved 2026-09-21 (spec 018)**: the view reports splices (UTF-16 offsets, old text, document order), Rust applies them to a mirror and the document takes the mirror; a 3 MB file registers a keystroke in 87 ms. Remaining O(n): the LSP `didChange` still sends the full text, and the document is a `String` (rope later) | resolved | 2 |

## Conventions
- **Status**: open · investigating · decided · resolved · wontfix.
- Link the ADR if the resolution became a decision.
- Include measurements, not adjectives ("30 fps at 50k nodes on WebGL2", not "slow").
