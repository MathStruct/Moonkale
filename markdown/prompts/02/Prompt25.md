

Got a message from a friend:

I am currently testing a build I compiled from source on macOS. It seems like the keybindings are a mix of those intended for Linux and Windows. For example, pressing Ctrl + O acts like Cmd + O, and pressing Ctrl + N acts like Cmd + N. macOS users use Ctrl + N for Emacs-style cursor navigation. I would really appreciate it if this behavior could be fixed.

If you can fix it, please fix if not then document it for later to be resolved once we have a MacOS environmet to test it on?

(Can we run MacOS inside Docker?)



Else I really wanted to hand out some builds now, for that should I just run:
git tag v0.1.0 && git push --tags
?