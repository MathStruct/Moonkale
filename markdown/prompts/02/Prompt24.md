Behind the Extension button there are already checkboxes which extension is enabled. So there is no reason to duplicate this in the settings. The settings should selectors for which extension is actually used if 
The settings of an extension should belong to the extension itself.

It should be possible to save multiple ssh connections and select them in a dropdown menu.

It would be good if one can have multiple agents saved even if only one is running.
- Multiple Agents running at once would definitely be a requirement. (Please document multiple concurrent agents if it is not too much hassle please implement.)

Please make it such that the history of sessions is part of the agent window.
Please make it such that one can select which of the saved agents one actually runs. 


Claude Code does not really run. Typically It should open the browser with a website where one can register with Claude. Then one can get a Code from there to put into the dialog for the claude Agent. Currently that is not possible, so other agents Which hand out an API key work but not Claude.